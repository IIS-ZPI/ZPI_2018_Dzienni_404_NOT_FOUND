language: java
install: true
sudo: false
script: mvn clean verify

  
install:
  - mvn install -DfinalName=release -DskipTests=true -Dmaven.javadoc.skip=true -B -V
    
deploy:
  provider: releases
  user: "fujitsusiemens3@o2.pl"
  password: $PASS
  api_key: $SECURE_KEY
  file: target/release.jar
  skip_cleanup: true
  on:
    tags: true
